import spock.lang.Specification

import java.lang.Void as Should

class RunnableSpec extends Specification {

    Should "create and run new thread using runnable interface" () {

    }

    Should "create and run new thread on the same thread using runnable interface" () {

    }

}