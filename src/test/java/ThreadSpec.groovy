import spock.lang.Specification

import java.lang.Void as Should

class ThreadSpec extends Specification {

    Should "create and run new thread using Thread class" () {

    }

    Should "create and run new thread on the same thread using Thread class" () {

    }

    Should "create and run new thread using Thread class with custom name" () {

    }

}